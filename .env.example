# Copy to .env and fill in when you're ready to run the agent.
# The stack builds and boots WITHOUT this key; only agent turns need it.
ANTHROPIC_API_KEY=

# Optional overrides
CLAUDE_MODEL=claude-opus-5
MAX_TOKENS=4096
