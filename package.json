{
  "name": "voice-sales-agent",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Continuous voice interface (Inworld STT/TTS) for the wholesale sales agent.",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/audio.js && node --check src/agent-client.js && node --check public/voice.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
